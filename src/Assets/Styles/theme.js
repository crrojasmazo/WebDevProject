import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1A1A1D',
    },
    secondary: {
      main: '#C3073F',
    },
    background: {
      default: '#1D366F',
    },
    error: {
      main: '#ff1744',
    },
  },
});

export default theme;
