import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1D366F',
    },
    secondary: {
      main: '#08C076',
    },
    background: {
      default: '#1D366F',
    },
  },
});

export default theme;
